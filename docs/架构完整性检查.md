# LinScio MedComm 架构完整性检查

**检查基准**：4.1 架构图、4.2 形式路由器  
**检查日期**：2026-03-22

---

## 一、Electron 主进程

| 能力 | 要求 | 当前状态 | 说明 |
|------|------|----------|------|
| 标题栏 / 窗口管理 | 基础窗口 | ✅ | BrowserWindow 已配置 |
| spawn Python 子进程 | 注入 LINSCIO_APP_DATA | ✅ | startBackend + env |
| 健康检查 | 后端存活探测 | ✅ | 每 15s 轮询 /health |
| 崩溃重启 | 子进程重启 | ✅ | on('exit') 重试，最多 5 次 |
| Keychain 读写 | keytar | ✅ | keychain.js + IPC，Settings 保存 |
| medcomm-image 协议 | 图像安全访问 | ✅ | protocol.handle 已注册 |
| Alembic 迁移前备份 | 失败回滚 | ✅ | init_db 备份 + run_migrations.py |
| 平台 wheels 离线安装 | — | ❌ | 未实现 |

---

## 二、Chromium 渲染进程（Vue 3 SPA）

| 能力 | 要求 | 当前状态 | 说明 |
|------|------|----------|------|
| 五栏式布局 | 多栏布局 | ⚠️ | 当前为侧边栏 + 主内容两栏 |
| 形式感知编辑器 | 按 content_format 切换节点 | ✅ | useFormatEditor + 6 扩展 |
| medcomm-image 协议 | 访问本地图像 | ✅ | web 模式用 /serve 代理 |

---

## 三、FastAPI 后端

| 能力 | 要求 | 当前状态 | 说明 |
|------|------|----------|------|
| 形式路由器 | content_format → Agent/章节/导出器 | ✅ | 已按形式路由 |
| LangGraph 引擎 | 写作图 + 图像生成图 | ✅ | medcomm_graph + image_graph |
| 防编造四道防线 | 声明核实/数据占位/绝对化/阅读难度 | ✅ | pipeline 已实现 |
| 三层增强系统 | RAG + Few-shot + 术语 | ✅ | prompt_builder 已实现 |
| 图像生成引擎 | 多风格 | ✅ | DALL·E 3 + Pollinations |
| 格式导出引擎 | 按形式路由 | ✅ | ScriptExporter/ComicExporter 等已按 content_format |
| SQLite WAL | 分域写锁 | ✅ | |
| sqlite-vec | 向量检索 | ❌ | 未实现 |
| FTS5 | 全文检索 | ✅ | knowledge 已索引 |

---

## 四、形式路由器（4.2 FORMAT_CONFIG）

### 4.1 配置完整性 ✅

FORMAT_CONFIG 已覆盖 17 种形式，与 4.2 一致：

- agent：17 组 Agent 名称已配置
- exporter：HtmlDocxExporter / ScriptExporter / StoryboardExporter / ComicExporter / CardExporter / PdfExporter / LongImageExporter / TxtExporter
- image_style：comic / flat_design / medical_illustration / picture_book

### 4.2 配置落地情况

| 配置项 | 落地位置 | 状态 |
|--------|----------|------|
| agent | agents/registry.py | ✅ FormatAgent 按 17 组路由 |
| section_schema | format_router SECTION_TYPES_BY_FORMAT | ✅ |
| exporter | export/router.py | ✅ 按 content_format 选 ScriptExporter 等 |
| image_style | imagegen/engine.py | ✅ 已用 style 参数 |

---

## 五、缺口汇总

### P0 核心架构 ✅

1. ~~**导出按形式路由**~~：已实现
2. ~~**Agent 按形式路由**~~：已实现

### P1 主进程增强 ✅

3. ~~**健康检查**~~：已实现
4. ~~**崩溃重启**~~：已实现
5. ~~**Alembic 迁移前备份**~~：已实现

### P2 可选

6. ~~**LangGraph 图像生成图**~~：已实现 image_graph
7. ~~**Keychain**~~：已实现 keytar + Settings
8. **五栏布局**：若产品要求五栏式编辑界面

---

## 六、架构符合度概览

| 层级 | 符合 | 部分/骨架 | 未实现 |
|------|------|-----------|--------|
| Electron | 窗口、spawn、medcomm-image、健康检查、崩溃重启、Keychain、Alembic 备份 | — | wheels 离线安装 |
| Vue | 形式感知编辑器、协议 | 两栏布局（非五栏） | — |
| FastAPI | 形式路由、防编造、增强、FTS5、图像引擎、导出/Agent 按形式、图像图 | — | sqlite-vec |
| 形式路由器 | FORMAT_CONFIG 完整、agent/exporter 已落地 | — | — |
