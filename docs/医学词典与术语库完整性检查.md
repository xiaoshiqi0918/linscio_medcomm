# 医学词典与术语库 - 开发完整性检查

基于 v4.0 规范：词典注入策略按受众分级。

---

## 规范要求

| 受众 | 注入内容 |
|------|----------|
| `public` / `patient` | `layman_explain` + `analogy`（通俗解释 + 比喻） |
| `student` / `professional` | 标准术语 + 缩写 |

---

## 当前状态

| 子项 | 状态 | 说明 |
|------|------|------|
| medical_terms 表 | ✅ | term, layman_explain, analogy, audience_level, abbreviation, specialty |
| abbreviation 字段 | ✅ | 已新增，student/professional 注入用 |
| TermInjector 接入 DB | ✅ | 按 topic、audience_level、specialty 检索 |
| 按受众分级注入 | ✅ | public/patient → layman_explain+analogy；student/professional → term+abbreviation |
| 术语管理 API | ✅ | GET/POST/GET:id/DELETE /api/v1/terms |
| 种子数据 | ✅ | scripts/seed_medical_terms.py，11 条 |

---

## 缺口（已补齐）

1. ~~MedicalTerm 模型~~ → 增加 abbreviation、specialty
2. ~~TermInjector~~ → 接入 medical_terms，AUDIENCE_LEVEL_MAP 映射
3. ~~prompt_builder~~ → 按 target_audience 分支格式化
4. ~~术语 API~~ → GET 列表、POST 新建、GET 详情、DELETE
5. ~~种子数据~~ → seed_medical_terms.py
