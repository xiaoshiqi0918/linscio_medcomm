# Binary mirrors for China (avoids GitHub ETIMEDOUT during electron postinstall).
# If you are outside CN and prefer official hosts, delete these two lines or override in shell.
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
